import React from "react";
import ReactDOM from "react-dom/client";
import JackFeedingTracker from "./App.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <JackFeedingTracker />
  </React.StrictMode>
);
